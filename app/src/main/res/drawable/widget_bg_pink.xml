<?xml version="1.0" encoding="utf-8"?>
<!-- Morning: the day is still open, so it reads as an invitation rather than a warning. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="24dp" />
    <gradient
        android:angle="270"
        android:endColor="#C81E7E"
        android:startColor="#E5459F"
        android:type="linear" />
</shape>
